import { ID } from './ID';
import { IDs } from './IDs';
import { New } from "../New";
import { defaults } from "./defaults";
import { Time } from './Time';

export const entity = {
  ID,
  IDs,
  New,
  defaults,
  Time,
}
